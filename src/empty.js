/** @param {NS} ns */
export async function main(ns) {
    // As you can probably tell, this script does absolutely nothing, and
    // is only used for the "Need more real life ram" achievement
  
    while (true) {
      await ns.sleep(1000);
    }
  }